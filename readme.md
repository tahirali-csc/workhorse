Prerequisites

Docker
Go Installation

How to run this app on local machine

1. From the application directory, open a new terminal tab and run
```bash
./worker/worker.sh
```

2. From the application directory, open a new terminal tab and run
```bash
./server/server.sh
```

3. From the application directory, open a new terminal tab and run
```bash
./client/client.sh
```